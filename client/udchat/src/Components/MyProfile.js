import React from 'react';
import Layout from './Layout';

const MyProfile = () => {

return (
    <Layout>
        <div>
        <h2>This is the profile page, log in to access its contents</h2>
        </div>
    </Layout>
      );
    };
  
export default MyProfile;